<h3>Home</h3>
<p>This is a test application for the Box OAuth2 API.</p>
<% if user_signed_in? -%>
    <p>You are currently signed in as <%= current_user.name %>.</p>
<% else %>
    <p>You are not currently signed in. <%= link_to "Sign in with Box", user_box_oauth2_omniauth_authorize_path %></p>
<% end %>
